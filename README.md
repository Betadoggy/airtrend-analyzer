# 미래 공항산업 트랜드 키워드 분석
main.py를 실행

### 명령어
pandas, newspaper4k, scikit-learn, requests, beautifulsoup4 라이브러리 설치
```
pip install pandas newspaper4k scikit-learn requests beautifulsoup4
```

### 윈도우 인증서 관리자에서 추출하기
1. `Win + R` 키를 누르고 `certlm.msc`를 입력하여 실행합니다.
2. 신뢰할 수 있는 루트 인증 기관 → 인증서 폴더를 클릭합니다.
3. 목록에서 회사 보안 관련 이름(예: Somansa, Company Root CA 등)을 찾습니다.
4. 해당 인증서 우클릭 → 모든 작업 → 내보내기.
5. 인증서 내보내기 마법사에서 **'Base-64로 인코딩된 X.509(.CER)'**를 선택하여 저장합니다 

### 내가 까먹을까봐 넣은 git 명령어
stage
```
git add .
```

commit
```
git commit -m "messsage"
```

push
```
git push
```

repository clone
```
git clone https://github.com/username/project-name.git
```